# ~/.config/git/config.local
# This file is ignored by git (via .chezmoiignore or .gitignore strategies usually,
# but here it is simply not tracked by chezmoi by default unless added).
# Use this file to store your private git configuration.

[user]
    name = Your Name
    email = your.email@example.com
    
    # SSH Signing Key Setup (Optional):
    # 1. Generate or retrieve your SSH public key:
    #    ppget show "SSH Key Name" --field public_key > ~/.ssh/id_ed25519.pub
    # 2. Set the signing key:
    #    signingkey = ~/.ssh/id_ed25519.pub
    # 3. Create allowed_signers file:
    #    echo "your.email@example.com $(cat ~/.ssh/id_ed25519.pub)" > ~/.config/git/allowed_signers
    # 4. Uncomment the lines below:
    # signingkey = ~/.ssh/id_ed25519.pub

# [gpg "ssh"]
#     allowedSignersFile = ~/.config/git/allowed_signers

# [credential]
#     helper = osxkeychain
