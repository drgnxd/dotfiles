# ~/.config/zsh/.zsh_options
# Zshのオプションとシェル変数を設定

# ------------------------------------------------------------------------------
# 履歴 (History)
# ------------------------------------------------------------------------------
# 履歴ディレクトリの作成
[ -d "$XDG_STATE_HOME/zsh" ] || mkdir -p "$XDG_STATE_HOME/zsh"
# メモリに保存する履歴の件数
HISTSIZE=10000
# 履歴ファイルに保存する件数
SAVEHIST=10000
# 履歴を共有 (複数のターミナルで履歴を即時共有)
setopt share_history
# 履歴をインクリメンタルに保存
setopt inc_append_history
# 重複する履歴を保存しない
setopt hist_ignore_dups
