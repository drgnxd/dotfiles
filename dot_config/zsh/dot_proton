# ~/.config/zsh/.proton
# Proton Pass configuration

# Proton Passの鍵をSSHエージェントにロード
# Background execution to avoid blocking shell startup
if command -v pass-cli >/dev/null 2>&1; then
    pass-cli ssh-agent load >/dev/null 2>&1 &!
fi
