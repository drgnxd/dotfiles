# Bootstrap for XDG Base Directory compliance
# This file is placed as $HOME/.zshenv

# 1. Define XDG_CONFIG_HOME (with fallback if undefined)
export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"

# 2. Change Zsh configuration directory to under XDG ($HOME/.config/zsh)
#    This ensures subsequent files like .zshrc are loaded from here
export ZDOTDIR="$XDG_CONFIG_HOME/zsh"

# 3. Source the actual .zshenv if it exists (for environment variable definitions etc.)
if [[ -f "$ZDOTDIR/.zshenv" ]]; then
    source "$ZDOTDIR/.zshenv"
fi